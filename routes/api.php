<?php

// All API routes now live in routes/web.php (under the "auth"/"web"
// middleware group, prefixed with /api) so they share the logged-in
// user's session. This file is intentionally left empty.
